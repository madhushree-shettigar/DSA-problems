class Solution:
    def firstPalindrome(self, words: List[str]) -> str:
        # Iterate through each word in the array
        for word in words:
            # Check if the word is a palindrome
            if word == word[::-1]:
                return word  # If palindrome, return the word
        
        return ""  # If no palindrome found, return an empty string
